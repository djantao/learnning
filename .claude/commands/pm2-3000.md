Start learnning-3000 (3000) and open logs.
```bash
cd "d:\daima\vscode\learnning" && pm2 start ecosystem.config.cjs --only learnning-3000 && start wt.exe -d "d:\daima\vscode\learnning" pwsh -NoExit -c "pm2 logs learnning-3000"
```
